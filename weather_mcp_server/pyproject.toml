[project]
name = "weather_mcp_server"
version = "0.1.0"
description = "A MCP server that exposes two tools: `get_alerts` and `get_forecast`."
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "httpx>=0.28.1",
    "mcp[cli]>=1.27.0",
]
